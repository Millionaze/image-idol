# Workflow & Pipeline Engine — Backend Build\n\nThis is the backend-only foundation for HighLevel-style automation in Pixel Growth. No new UI beyond a minimal debug page. Visual canvas + kanban come in a follow-up prompt.\n\n## What you get when this lands\n\n- An `events` bus that every part of the app writes to\n- A workflow engine that listens to events, enrolls contacts, and walks a JSON DAG of actions/waits/branches\n- Pipelines built on top of the existing `contacts` table (no separate \